constraint
- 架构模式：Monorepo (Bun Workspaces)
- 技术栈：
    - Web 端：React + Tailwind CSS + ShadcnUI（可选）
    - 后端：Hono + Bun + 某些数据库 + Better-Auth（可选）
    - 移动端（当需求中提到时）：Flutter + Signals + Flutter Hooks + fquery
    - 测试：Bun Test (后端) + Playwright (E2E)

- 开发原则：
    - 一次只聚焦于一个阶段，必须在前一个阶段通过**自动化验证**之后才能去开发下一个阶段。
    - **禁止盲写代码**：任何功能代码编写前，必须先确认测试环境可用。

- 测试编写要求（让每一步都可验证）：
    - **后端新增 API**：必须在 `packages/server/src/routes/__test__/api.test.ts` 中编写对应测试
    - **前端新增页面**：必须在 `.e2e-tests/smoke.playwright.spec.ts` 中编写对应测试
    - 测试必须包含错误检测，确保无控制台 error 和 JS 异常
- 开发流程
    - 理解用户需求并分解任务，确保每次的任务可验证
    - 得出总共n个任务
    - 进入开发循环
- 开发循环：
    1. 开始撰写第k个任务的代码
    2. 编写后端 API + 测试
    3. 运行 `bun test` 验证后端
    4. 编写前端页面 + E2E 测试
    5. 运行 `npx playwright test` 验证前端
    6. 如果成功 → git add & commit → 下一个（k+1个）任务

- **不要自己运行 bun dev**，初始化项目后，通知人类运行

- 文档规范：
    - 每个功能最多写一个文档，存放在 `doc/` 目录下
    - 禁止同一功能出现多个独立文档

- 版本控制与进度记录：
    - 每隔完整改动后立即 `git add & commit`
    - 确保 `node_modules` 和构建产物在 `.gitignore` 中
    - **PROGRESS.md 记录规则**：
        - 每次遇到问题或完成重要改动后记录
        - 格式：问题描述 → 解决方案 → 复盘预防 → Commit ID
